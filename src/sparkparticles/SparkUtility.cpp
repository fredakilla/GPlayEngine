#ifndef GP_NO_SPARK

#include "../sparkparticles/SparkUtility.h"
#include "../sparkparticles/SparkQuadRenderer.h"
#include <spark/Core/IO/SPK_IO_Manager.h>

#include "../math/Vector3.h"

namespace SPK {
namespace GP3D {

    ////////////////////////////
    /// Conversion functions ///
    ////////////////////////////

   /* gplay::Vector3 spk2gp3d(const Vector3D& v)
    {
        return gplay::Vector3(v.x, v.y, v.z);
    }

    SPK::Vector3D gp3d2spk(const gplay::Vector3& v)
    {
        return Vector3D(v.x, v.y, v.z);
    }*/

}}

#endif
